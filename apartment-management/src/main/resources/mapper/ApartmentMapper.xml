<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huawei.apartment.mapper.ApartmentMapper">

    <!-- 公寓列表VO结果映射 -->
    <resultMap id="ApartmentListVOResultMap" type="com.huawei.apartment.vo.ApartmentListVO">
        <id column="apartment_id" property="apartmentId"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="city" property="city"/>
        <result column="address" property="address"/>
        <result column="longitude" property="longitude"/>
        <result column="latitude" property="latitude"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="room_count" property="roomCount"/>
        <result column="vacant_count" property="vacantCount"/>
        <result column="rented_count" property="rentedCount"/>
        <result column="maintenance_count" property="maintenanceCount"/>
        <result column="booked_count" property="bookedCount"/>
    </resultMap>

    <!-- 分页查询公寓列表 -->
    <select id="selectApartmentPage" resultMap="ApartmentListVOResultMap">
        SELECT
            a.apartment_id,
            a.name,
            a.code,
            a.city,
            a.address,
            a.longitude,
            a.latitude,
            a.phone,
            a.status,
            a.created_at,
            COUNT(r.room_id) AS room_count,
            SUM(CASE WHEN r.status = 0 THEN 1 ELSE 0 END) AS vacant_count,
            SUM(CASE WHEN r.status = 1 THEN 1 ELSE 0 END) AS rented_count,
            SUM(CASE WHEN r.status = 2 THEN 1 ELSE 0 END) AS maintenance_count,
            SUM(CASE WHEN r.status = 3 THEN 1 ELSE 0 END) AS booked_count
        FROM apartment a
        LEFT JOIN room r ON a.apartment_id = r.apartment_id AND r.deleted_at IS NULL
        WHERE a.deleted_at IS NULL
        <if test="name != null and name != ''">
            AND a.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="city != null and city != ''">
            AND a.city LIKE CONCAT('%', #{city}, '%')
        </if>
        <if test="status != null">
            AND a.status = #{status}
        </if>
        GROUP BY a.apartment_id
        ORDER BY a.created_at DESC
    </select>

    <!-- 根据公寓编码查询 -->
    <select id="selectByCode" resultType="com.huawei.apartment.entity.ApartmentEntity">
        SELECT *
        FROM apartment
        WHERE code = #{code}
          AND deleted_at IS NULL
        LIMIT 1
    </select>

    <!-- 统计公寓下的楼栋数量 -->
    <select id="countBuildingsByApartmentId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM building
        WHERE apartment_id = #{apartmentId}
          AND deleted_at IS NULL
    </select>

    <!-- 统计公寓下的房间数量 -->
    <select id="countRoomsByApartmentId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM room
        WHERE apartment_id = #{apartmentId}
          AND deleted_at IS NULL
    </select>

</mapper>
